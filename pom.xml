<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- Spring Boot Parent: This ensures all microservices use compatible dependency versions -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.1</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>

    <groupId>com.securegateway</groupId>
    <artifactId>secure-llm-gateway-parent</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    
    <!-- "pom" packaging means this project doesn't compile to a JAR, it just manages modules -->
    <packaging>pom</packaging>
    <name>Secure LLM Gateway Parent</name>
    <description>Parent POM for the Secure Enterprise LLM Gateway microservices</description>

    <properties>
        <java.version>21</java.version>
        <spring-cloud.version>2023.0.2</spring-cloud.version>
    </properties>

    <!-- These are the microservices we will create -->
    <modules>
        <module>api-gateway</module>
        <!-- <module>core-ai-service</module>
        <module>audit-service</module> -->
    </modules>

    <!-- Dependency Management for Spring Cloud (used for Gateway and microservice networking) -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

</project>